# Puerto del servidor WebSocket
PORT=8080

# Orígenes permitidos separados por coma. Ejemplo: http://localhost:3000,http://192.168.1.10:3000
ALLOWED_ORIGIN=http://localhost:3000

# Si quieres exigir autenticación (token Firebase) para unirte al chat
REQUIRE_AUTH=false

# Configuración Firebase Admin (usa UNA de las dos opciones)
# Opción A: Ruta al JSON de credenciales del servicio
# GOOGLE_APPLICATION_CREDENTIALS=C:\\ruta\\a\\service-account.json

# Opción B: Variables de entorno (útil en hosting o sin archivo)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
# IMPORTANTE: si pegas la llave en .env, reemplaza saltos de línea por \n
FIREBASE_PRIVATE_KEY=
